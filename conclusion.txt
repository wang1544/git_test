after add a file to github, and then add it to .gitignore, the connection between the local file and github file are still there. And the git status will still show changes.

Solution: manually delete it from GitHub web